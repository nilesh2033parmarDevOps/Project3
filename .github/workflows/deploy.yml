name: Auto Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 187.127.135.184
          username: root
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd ~/Project3
            ./deploy.sh